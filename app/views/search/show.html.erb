<div class="container">
  <div class="divider"></div>
  <div class="section">
    <h5 class="center-align">Search our Database, Get Real Numbers</h5>
    <div class="row">
      <div class="col s12 m12">
        <div class="card-panel teal">
        <span class="white-text">How it works:<br> Select a value for each of the fields below, Year, Insitution, Program and Ethnicity. 
        You receive the number of graduates for that field per your query. 
        The overall number of graduates for your choosen program.
        A run of your ethnicity field and program against the program.
        A run of your ethnicity field and program against the whole instiution for your choosen ethnicity.
        </span>
      </div>
    </div>
  </div>
  </div>
  <div class="form">
  <% cache "form" do%>
  <%= form_tag :results, method: :get do%>
      <%= select_tag 'year', options_from_collection_for_select(@years, :date_year, :date_year, include_blank: true), class:"browser-default"%>      
      <%= select_tag 'program', options_from_collection_for_select(@programs, :cip2, :name, include_blank: true), class:"browser-default"%>
      <%= select_tag 'college', options_from_collection_for_select(@institutions,:name, :name, include_blank: true), class:"browser-default"%>
      <%= select_tag 'ethnicity', options_from_collection_for_select(@ethnicities,:ethnicity, :ethnicity, include_blank: true), class:"browser-default"%>
      <p style="color: red;"><%=flash[:notice]%></p>
      <%= submit_tag "Get Results"%>
    <% end %>
    <% end %>
  </div>
</div>
